{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "stack build && stack exec Logimaster-exe"
        },
        {
            "label": "test and coverage",
            "type": "shell",
            "command": "stack test --coverage && firefox $(stack path --local-hpc-root)/index.html"
        }
    ]
}